repositories:
  transform:
    - dbt_hubspot
  source:
    - dbt_hubspot_source
  utils:
    - dbt_fivetran_utils

require-dbt-version: [">=0.18.0", "<0.19.0"]

packages:
  fishtown-analytics/dbt_utils: [">=0.7.0", "<0.8.0"]

ci-dbt-version: 0.18.0